<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@id/settings_fragment"
        android:icon="@android:drawable/ic_menu_preferences"
	android:title="@string/settings"
	android:visible="@string/settings_enabled"
	/>

    <item
        android:id="@+id/clear_support_files"
        android:title="@string/clear_support_files"
        android:visible="@string/clear_support_files_enabled"
        app:showAsAction="never" />

    <item
        android:id="@id/help_fragment"
        android:icon="@android:drawable/ic_menu_help"
        android:visible="@string/help_enabled"
        android:title="@string/help" />

    <item
        android:id="@+id/option_wiki"
        android:title="@string/wiki"
        android:visible="@string/wiki_enabled"
        app:showAsAction="never" />

    <item
        android:id="@+id/terms_and_conditions"
        android:title="@string/toc"
        android:visible="@string/toc_enabled"
        app:showAsAction="never" />

</menu>
